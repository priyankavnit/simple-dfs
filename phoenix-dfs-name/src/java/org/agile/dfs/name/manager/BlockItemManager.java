package org.agile.dfs.name.manager;

import org.agile.dfs.core.entity.BlockItem;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class BlockItemManager {
    private final static Logger logger = LoggerFactory.getLogger(BlockItemManager.class);

    public BlockItem locate(String fileId, String blockNo) {

        return null;
    }
}
